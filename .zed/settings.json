{
  "languages": {
    "JavaScript": {
      "formatter": {
        "external": {
          "command": "bun",
          "arguments": ["run", "--cwd", "web", "oxfmt", "--stdin-filepath", "{buffer_path}"]
        }
      }
    },
    "TypeScript": {
      "formatter": {
        "external": {
          "command": "bun",
          "arguments": ["run", "--cwd", "web", "oxfmt", "--stdin-filepath", "{buffer_path}"]
        }
      }
    },
    "TSX": {
      "formatter": {
        "external": {
          "command": "bun",
          "arguments": ["run", "--cwd", "web", "oxfmt", "--stdin-filepath", "{buffer_path}"]
        }
      }
    },
    "JSON": {
      "formatter": {
        "external": {
          "command": "bun",
          "arguments": ["run", "--cwd", "web", "oxfmt", "--stdin-filepath", "{buffer_path}"]
        }
      }
    },
    "JSONC": {
      "formatter": {
        "external": {
          "command": "bun",
          "arguments": ["run", "--cwd", "web", "oxfmt", "--stdin-filepath", "{buffer_path}"]
        }
      }
    },
    "Vue.js": {
      "formatter": {
        "external": {
          "command": "bun",
          "arguments": ["run", "--cwd", "web", "oxfmt", "--stdin-filepath", "{buffer_path}"]
        }
      }
    },
    "Markdown": {
      "formatter": {
        "external": {
          "command": "bun",
          "arguments": ["run", "--cwd", "web", "prettier", "--stdin-filepath", "{buffer_path}"]
        }
      }
    }
  }
}
